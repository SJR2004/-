# fly.toml — Fly.io 설정 파일
app = "stock-predict-model"   # ← 여기만 본인 앱 이름으로 변경

[build]
  dockerfile = "Dockerfile"

[env]
  PORT = "8080"

[[services]]
  internal_port = 8080
  protocol = "tcp"

  [[services.ports]]
    port = 80
  [[services.ports]]
    port = 443
